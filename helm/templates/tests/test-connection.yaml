apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vault-kms-provider.fullname" . }}-test-connection"
  labels:
    {{- include "vault-kms-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vault-kms-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
