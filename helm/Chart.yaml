apiVersion: v2
maintainers:
  - name: ruddickmg
    email: ruddickmg@gmail.com
name: vault-kms-provider
description: A Helm chart for deploying the vault-kms-provider. vault-kms-provider is a plugin allowing vault to be used as a kms service for kubernetes encryption at rest
type: application
version: 0.2.2
appVersion: "0.2.2"
